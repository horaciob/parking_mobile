== README
tested on ruby 2.2.1p85

Dependencies:

Mysql

To install

gem install bundler

bundle install

rake db:create:all
rake db:migrate

if all is ok 

rspec 

should run test and if all pass.

rails s
curl localhost:3000
